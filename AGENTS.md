# SupportDeskAgent 开发规范

本文档约束本仓库后续的设计、编码、测试和文档工作。任何新功能都应遵循这些规则；如果确实需要例外，必须在代码评审或相关设计文档中说明原因和影响。

## 语言与注释

- README、架构说明、部署文档、接口说明和面向开发者的操作指南统一使用中文。
- 代码的关键流程、复杂分支、边界条件和不直观的设计取舍必须使用自然、准确的中文注释。
- 每个核心模块至少要说明模块职责；涉及状态流转、跨层数据传递、异步调用、缓存、权限、降级或外部副作用时，还要说明数据从哪里来、由谁维护以及失败时如何处理。
- 前端页面应在主要业务区域、状态来源、关键交互和后续接口接入点添加中文注释；后端应在事务边界、领域规则、Graph 路由、工具调用和异常恢复位置添加中文注释。
- 注释应优先解释“为什么这样设计”和“这里必须满足什么约束”，必要时再说明“代码做了什么”，确保不了解上下文的开发者也能快速理解关键链路。
- 不给变量赋值、普通函数调用等显而易见的代码堆砌注释，避免注释反而干扰阅读。
- 类、公共函数和公共模块根据复杂度补充中文文档字符串，重点说明职责、输入输出和异常行为。
- LangGraph、RAG、Checkpoint、Tool Calling 等行业术语保留标准英文，首次出现时可补充中文解释。

## 长期工程原则

- 优先选择可维护、可测试、可观测和可扩展的实现，不用临时代码绕过应有的工程机制。
- 不因为当前功能简单，就放弃已经明确会持续需要的基础能力，例如数据库迁移、类型校验、状态管理、配置管理、日志、测试和依赖锁定。
- 同时避免没有明确需求的过度设计。引入框架或中间件前，应确认它解决了真实问题，并记录关键技术取舍。
- 业务规则、Agent 编排、数据访问和接口层保持清晰边界，禁止把所有逻辑堆积在路由、组件或单个 Graph 节点中。
- 所有可能产生外部副作用的操作都要考虑幂等性、超时、重试、审计和失败恢复。
- 涉及退款、补偿、权限、数据删除等高风险动作时，必须经过明确的权限校验或 Human-in-the-loop 人工审批。

## 教学式协作与手写代码

- 本项目以用户亲自手写代码为默认开发方式。除非用户在当前任务中明确要求“直接实现”或“帮我修改代码”，否则不得直接新增、修改或删除业务代码。
- 每次提出或开始一个新功能前，先在 `docs/features/` 下创建独立的中文 Markdown 实现文档。文档名称使用有序编号和功能名称，例如 `001-最小客服Agent.md`，便于按学习顺序长期维护。
- 功能文档必须先讲设计，再讲代码。至少包含业务目标、完成范围、不做什么、前置知识、调用链、数据流、文件职责、设计取舍、风险点、实现顺序、验证方法和完成标准。
- 实现顺序必须细化到文件级和步骤级，明确说明先写哪个文件、再写哪个文件，以及后一文件依赖前一文件的原因。不能只给出笼统的任务列表。
- 对每个需要新建或修改的文件，都要说明它位于该目录的原因、它只负责什么、它不应该负责什么，以及它与其他模块之间如何传递数据。
- 功能文档必须提供经过验证的完整实现代码，不能只给函数签名、伪代码或留空的 TODO 骨架。代码应包含必要的类型、业务逻辑、异常处理和自然的中文注释，达到用户逐行手写后即可运行的程度。
- 文档中的完整代码应先在不覆盖用户业务文件的隔离副本或测试环境中完成静态检查和必要测试，再记录验证命令与结果；如果受外部服务、密钥或环境限制无法完成某项验证，必须明确说明未验证范围，不能声称已经通过。
- 每完成一个步骤，都要提供一个最小验证方法，例如预期导入结果、测试命令、接口响应或页面行为，让用户能够边写边确认，而不是全部写完后一次排错。
- 用户提交自己写的代码后，优先进行教学式代码审查：指出问题、解释原因、给出修改方向。除非用户明确授权，不直接替用户改正。
- 如果新功能需要新增依赖，只在功能文档中说明依赖名称、用途和选择理由，并更新依赖清单；仍需等待用户自行安装。

## 后端约束

- 使用 Pydantic 定义接口、配置、工具参数和 LangGraph 结构化输出，避免在模块之间传递约定不明的字典。
- 使用 SQLAlchemy 管理数据访问，使用 Alembic 管理数据库结构变更，不通过手工 SQL 临时修改生产结构。
- I/O 密集型接口和工具优先采用异步实现；阻塞操作必须隔离，避免占用 FastAPI 事件循环。
- LangGraph State 只保存工作流运行所需状态，客户、工单、消息和审计记录等长期数据存入业务数据库。
- RAG 的切片、召回、重排序和生成阶段保持可替换、可评测，不把整条链路写成不可拆分的函数。
- 外部模型、订单系统、工单系统和第三方接口均通过适配层接入，核心业务不得直接依赖供应商特有响应结构。
- 数据库事务边界、异常类型和日志字段应明确，不静默吞掉异常。

## 前端约束

- 使用 TypeScript 严格模式，不使用无说明的 `any` 绕过类型系统。
- 服务端数据通过 TanStack Query for Vue 管理；真正跨页面、跨组件且不属于服务端缓存的客户端状态通过 Pinia 管理。
- 局部状态继续使用 Vue Composition API 的 `ref`、`reactive` 和 `computed`，不为了形式统一把所有状态放入 Pinia。
- 通用后台组件优先复用 Element Plus，业务组件继续保持清晰封装，避免直接堆叠大量组件库配置。
- 表单通过 Element Plus 表单能力与 Zod 进行输入校验，接口数据在系统边界处完成类型验证。
- 通用 UI、业务组件和页面容器分层组织，避免在页面文件中混合大量请求、状态和展示逻辑。
- 加载、空数据、错误、权限不足和网络重试状态必须有明确的用户反馈。

## 依赖与工具链

- 未经用户明确授权，不得执行 `pip install`、`uv add`、`npm install`、`pnpm install` 等依赖安装或升级命令。需要新增依赖时，先修改并说明依赖清单，再由用户确认和执行安装。
- 不得擅自向系统环境、全局环境或用户的虚拟环境安装任何包；验证工作应优先使用项目中已经存在的依赖。
- Python 依赖使用 `uv` 管理并生成锁文件；前端依赖使用 `pnpm` 管理并提交锁文件。
- 正式开发阶段禁止长期使用 `latest` 或完全不受约束的依赖版本，升级依赖必须经过测试。
- 使用 Ruff、mypy、ESLint 和 TypeScript 编译检查保持代码质量，并通过 pre-commit 或 CI 自动执行。
- 使用环境变量管理配置和密钥，仓库只提交不含真实凭证的 `.env.example`。
- 数据库、Redis 等本地基础设施通过 Docker Compose 提供一致的开发环境。

## 测试与质量

- 核心业务规则和 LangGraph 路由必须有单元测试。
- 数据库、RAG、Tool Calling 和 API 边界必须有集成测试。
- 客户咨询、自动建单、人工接管等关键用户流程必须有端到端测试。
- 修复缺陷时优先补充能够复现问题的测试，再实施修复。
- Agent 效果不能仅凭人工体验判断，需要维护评测数据集并跟踪检索、工具调用、安全和回答质量指标。
- 合并前至少通过格式检查、静态类型检查、单元测试和必要的集成测试。

## 文档与提交

- 新增重要模块时同步更新相关中文文档，不让代码与文档长期脱节。
- 架构决策存在明显取舍时，使用 ADR 或设计文档记录背景、方案、选择理由和后果。
- 提交内容保持单一目的，不混入无关格式化或重构。
- 不提交密钥、个人数据、模型生成的临时文件、运行日志和本地缓存。
- `docs/features/` 中的功能实现文档属于项目正式文档，应随代码演进同步更新；功能完成后补充实际实现差异、测试结果和可继续优化项。
